#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - prints the stdout
 * Return: 0 if successful
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
	printf("%d", i);
	}
	putchar('\n');
	return (0);
}
